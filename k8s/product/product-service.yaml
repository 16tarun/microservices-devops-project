apiVersion: v1
kind: Service

metadata:
  name: product-service
  namespace: shopsphere

spec:
  type: ClusterIP

  selector:
    app: product-service

  ports:
    - port: 5001
      targetPort: 5001